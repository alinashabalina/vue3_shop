import { defineComponent } from "vue";

export default defineComponent({
  name: "RectangleButton",
  props: {
    title: {
      type: String,
      required: true,
    },
  },
  components: {},
  setup() { },

  return: {},
});
